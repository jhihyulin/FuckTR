[pytest]
# 啟用即時輸出 log 到 terminal（非僅失敗時顯示）
log_cli = true
log_cli_level = DEBUG
log_cli_format = %(asctime)s [%(levelname)s] %(name)s - %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# 若需只在失敗時顯示 log，可改用：
# log_cli = false
# log_level = INFO
